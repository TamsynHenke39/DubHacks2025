# Payments Service - Environment Variables (sample)
# Copy this file to payments/.env and fill in the required values.
# This service is isolated; it will not modify your main app.

# Service metadata
PAYMENTS_SERVICE_NAME=payments
PAYMENTS_SERVICE_VERSION=0.1.0

# Runtime settings
PAYMENTS_CURRENCY=USD
# $500 max per transaction, expressed in cents
PAYMENTS_MAX_TX_CENTS=50000
# Port is set when you run uvicorn; this is informational
PAYMENTS_PORT=8001

# CORS (comma-separated origins). Leave blank to use defaults in code.
# Example: http://localhost:5173,http://127.0.0.1:5173
PAYMENTS_CORS_ALLOWED_ORIGINS=

# Database (SQLite)
# Relative path from repo root. Adjust if you prefer a different location.
DATABASE_URL=sqlite:///payments/payments.db

# Idempotency configuration
IDEMPOTENCY_TTL_SECONDS=86400

# CyberSource (Visa) - Sandbox credentials (OPTIONAL if you use Stripe)
# Refer to your CyberSource sandbox account for these values.
CYBERSOURCE_ENVIRONMENT=sandbox
# One of: http_signature or jwt. http_signature is common for hackathons.
CYBERSOURCE_AUTH_TYPE=http_signature
CYBERSOURCE_MERCHANT_ID=your_merchant_id
CYBERSOURCE_KEY_ID=your_rest_key_id
CYBERSOURCE_SHARED_SECRET=your_rest_shared_secret
# If you later choose JWT auth instead of HTTP Signature:
# CYBERSOURCE_JWT_KEY_ALIAS=
# CYBERSOURCE_JWT_KEY_PASSWORD=
# CYBERSOURCE_JWT_KEY_FILE= # path to .p12 or appropriate key file

# Stripe (Test Mode) - Server secret key (REQUIRED if you use Stripe)
# Get from Stripe Dashboard → Developers → API keys (sk_test_...)
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Notes:
# - Keep payments/.env out of version control. Add it to .gitignore if not already ignored.
# - Only use sandbox/test credentials for demos; never real card data.
